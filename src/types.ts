type Product = {
  id: number;
  title: string;
  description: string;
  price: number;
  image: string;
};

type Author = {
  name: string;
  bio: string;
  image: string;
};

export type { Product, Author };
